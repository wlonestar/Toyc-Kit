#include io

i64 main() {
  i64 a = 0;
  a = a++ + a++;
  printi64ln(a);
  i64 b = 0;
  b = ++b + ++b;
  printi64ln(b);
  f64 c = (i64) 1.132e2;
  printf64ln(c);
  i64 x = -1024 << 1;
  printi64ln(x);
  x = x >> 2;
  printi64ln(x);
  return 0;
}
