#include std

i64 main() {
  i64 a = 0;
  a = a++ + a++;
  printI64ln(a);
  i64 b = 0;
  b = ++b + ++b;
  printI64ln(b);
  f64 c = (i64) 1.132e2;
  printF64ln(c);
  i64 x = -1024 << 1;
  printI64ln(x);
  x = x >> 2;
  printI64ln(x);
  return 0;
}
