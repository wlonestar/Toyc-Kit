#include std

void test(i64 a, i64 b) {
  printi64(a + b);
  printspace();
}

i64 main() {
  i64 a = 0;
  while (a < 5) {
    i64 b = 0;
    while (b < 5) {
      test(a, b);
      b = b + 1;
    }
    a = a + 1;
  }
  printi64(a);
  return 0;
}
