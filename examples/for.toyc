extern void printI64(i64 val);
extern void printF64(f64 val);

void test(i64 a, i64 b) {
  printI64(a + b);
}

i64 main() {
  for (i64 a = 0; a < 5; a = a + 1) {
    for (i64 b = 5; b >= 0; b = b - 1) {
      test(a, b);
    }
  }
  for (i64 a = 0; a < 5; a = a + 1) {
    printI64(a);
  }
  return 0;
}
