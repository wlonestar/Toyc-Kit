i64 a = 0;
a
a = a++ + a++;
a
i64 b = 0;
b
b = ++b + ++b;
b
f64 c = 1.132e2;
c
i64 x = 1024;
x = -x;
x = x >> 2;
x
i64 inc(i64 a) { return a + 1; }
inc(inc(2))
f64 add(f64 a, f64 b) { return a + b; }
add(add(1, add(2, add(3, 4))))