extern void printI64(i64 val);
extern void printF64(f64 val);

i64 add(i64 x, i64 y) {
  return x + y;  
}

f64 mul(f64 x, f64 y) {
  return x * y;
}

// for test
void foo() {
}

// for test
i64 bar() {
  return 0;
}

i64 main() {
  i64 a = 1;
  if (a) {
    a = 2;
  } else {
    a = 3;
  }
  printI64(a);
  return 0;
}
