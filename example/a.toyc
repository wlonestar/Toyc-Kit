extern void printI64(i64 val);
extern void printF64(f64 val);

i64 add(i64 x, i64 y) {
  return x + y;  
}

f64 mul(f64 x, f64 y) {
  return x * y;
}

// for test
void foo() {}

// for test
i64 bar() {}

i64 main() {
  i64 a = 1;
  if (a) {
    a = 2 && 3 + 12;
  } else if (!a) {
    a = 3 - 54 || 132;
  } else {
    a = 4;
  }
  printI64(a);
}
