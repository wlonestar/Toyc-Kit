extern void printI64(i64 val);
extern void printF64(f64 val);

i64 add(i64 x, i64 y) {
  return x + y;  
}

f64 mul(f64 x, f64 y) {
  return x * y;
}

i64 main() {
  printI64(1);
  printI64(add(3, 4));
  printF64(1.1);
  printF64(mul(5, 3));
  printF64(add(12, 13) + mul(5, 3));
}
